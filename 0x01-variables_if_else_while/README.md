0. Positive anything is better than negative nothing

This program assigns a random number to the variable n each time it is executed. It uses 'srand' and 'RAND_MAX' to assign the random numbers.I have used "stlib.h" because of 'srand' and 'RAND_MAX' and "time.h" to assign the numbers differently at different times. I have used "stdio.h" for ouptus and inputs and it enables me to use the 'printf()'
I have used the if .. else statement to print out whthere the random number is positive, negative or a zero


